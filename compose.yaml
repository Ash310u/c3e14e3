services:
  ping-server:
    build:
      context: .
    image: ping-server:local
    container_name: ping-server
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      - PORT=${PORT:-8080}
    restart: unless-stopped
